spring:
  cloud:
    gateway:
      routes:
        - id: study-adapter
          uri: lb://study-adapter
          predicates:
            - Path=/api/auth/**,/api/system/**
          filters:
            - StripPrefix=1

# 获取路由地址：/actuator/gateway/routes